# ============================================
# 普洛狮ERP Fail2Ban Odoo 登录防护
# 安装位置: /etc/fail2ban/filter.d/odoo-login.conf
# ============================================

[Definition]
failregex = Login failed for db:.* login:.* from <HOST>
            Login error.* from <HOST>
ignoreregex =
